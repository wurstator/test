Run a test search using the scientific literature agent with the query: {query}

Use the default configuration (max 20 results, mistral-small model).
Display the results including search statistics, retrieval stats, and a summary.
